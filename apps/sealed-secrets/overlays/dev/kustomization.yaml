apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - ../../base

# This overlay is intentionally minimal — it's an application placeholder.
# Initialization (values, patches) will be added later.
